# patches/

Backport patches applied during the build. One patch per CVE.

Naming: `CVE-YYYY-NNNNN.patch` — filename must match the CVE it fixes.

Each patch should carry a header noting the upstream commit it was lifted
from and the CVE it addresses.
